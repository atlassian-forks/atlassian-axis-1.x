<?xml version="1.0" ?>
<!DOCTYPE project [
        <!ENTITY properties SYSTEM "../../../../../../xmls/properties.xml">
        <!ENTITY paths  SYSTEM "../../../../../../xmls/path_refs.xml">
        <!ENTITY taskdefs SYSTEM "../../../../../../xmls/taskdefs.xml">
        <!ENTITY taskdefs_post_compile SYSTEM "../../../../../../xmls/taskdefs_post_compile.xml">
        <!ENTITY targets SYSTEM "../../../../../../xmls/targets.xml">
]>

<!-- ===================================================================
<description>
   Test/Sample Component file for Axis

Notes:
   This is a build file for use with the Jakarta Ant build tool.

Prerequisites:

   jakarta-ant from http://jakarta.apache.org

Build Instructions:
   To compile
        ant compile
   To execute
        ant run

Copyright:
  Copyright (c) 2002-2003 Apache Software Foundation.
</description>
==================================================================== -->

<project default="compile">

<property name="componentName" value="test/wsdl/anytype" />
        &properties;
        &paths;
        &taskdefs;
        &taskdefs_post_compile;
        &targets;

<target name="clean">
    <echo message="Removing ${build.dir}/classes/${componentName} and ${build.dir}/work/${componentName}" />
    <delete dir="${build.dir}/classes/${componentName}"/>
    <delete dir="${build.dir}/work/${componentName}"/>
</target>

<target name="copy" depends="setenv"/>

<target name="compile" depends="copy">
  <echo message="Compiling test.wsdl.anytype"/>

    <!-- First, copy and compile the interface -->
    <copy todir="${build.dir}/work/test/wsdl/anytype" overwrite="yes">
      <fileset dir="${axis.home}/src/test/java/test/wsdl/anytype">
        <include name="AnyService.java"/>
      </fileset>
    </copy>

    <!-- Compile the interface first -->
    <javac srcdir="${build.dir}/work" destdir="${build.dest}" nowarn="${nowarn}" source="${source}" fork="${javac.fork}"
           debug="${debug}">
      <classpath refid="classpath" />
      <include name="test/wsdl/anytype/AnyService.java" />
    </javac>

    <!-- Generate WSDL from the interface -->
    <java2wsdl output="${build.dir}/work/test/wsdl/anytype/AnyService.wsdl"
               className="test.wsdl.anytype.AnyService"
               namespace="http://anytype.wsdl.test"
               location="http://localhost:8080/axis/services/AnyService">
        <classpath refid="classpath" />
        <classpath location="${build.dest}" />
    </java2wsdl>

    <!-- Generate Java classes from WSDL -->
    <wsdl2java url="${build.dir}/work/test/wsdl/anytype/AnyService.wsdl"
               output="${build.dir}/work"
               deployscope="session"
               serverSide="yes"
               skeletonDeploy="yes"
               noimports="no"
               verbose="no"
               testcase="no">
        <mapping namespace="http://anytype.wsdl.test" package="test.wsdl.anytype"/>
    </wsdl2java>

    <!-- Copy remaining source files, overwriting generated ones where needed -->
    <copy todir="${build.dir}/work/test/wsdl/anytype" overwrite="yes">
      <fileset dir="${axis.home}/src/test/java/test/wsdl/anytype">
        <include name="*TestCase.java"/>
        <include name="*Impl.java"/>
      </fileset>
    </copy>

    <!-- Compile all Java files -->
    <javac srcdir="${build.dir}/work" destdir="${build.dest}" nowarn="${nowarn}" source="${source}" fork="${javac.fork}"
           debug="${debug}">
      <classpath refid="classpath" />
      <include name="test/wsdl/anytype/*.java" />
    </javac>

</target>

<target name="run" depends="compile">
  <antcall target="execute-Component" />
</target>

</project>
