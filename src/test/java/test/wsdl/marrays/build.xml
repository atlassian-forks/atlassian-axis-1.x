<?xml version="1.0" ?>
<!DOCTYPE project [
        <!ENTITY properties SYSTEM "../../../../../../xmls/properties.xml">
        <!ENTITY paths  SYSTEM "../../../../../../xmls/path_refs.xml">
        <!ENTITY taskdefs SYSTEM "../../../../../../xmls/taskdefs.xml">
        <!ENTITY taskdefs_post_compile SYSTEM "../../../../../../xmls/taskdefs_post_compile.xml">
        <!ENTITY targets SYSTEM "../../../../../../xmls/targets.xml">
]>

<!-- ===================================================================
<description>
   Test/Sample Component file for Axis

Notes:
   This is a build file for use with the Jakarta Ant build tool.

Prerequisites:

   jakarta-ant from http://jakarta.apache.org

Build Instructions:
   To compile
        ant compile
   To execute
        ant run

Author:
  Matt Seibert mseibert@us.ibm.com

Copyright:
  Copyright (c) 2002-2003 Apache Software Foundation.
</description>
==================================================================== -->

<project default="compile">
<property name="componentName" value="test/wsdl/marrays" />
        &properties;
        &paths;
        &taskdefs;
        &taskdefs_post_compile;
	&targets;

<target name="clean">
    <echo message="Removing ${build.dir}/classes/${componentName} and ${build.dir}/work/${componentName}" />
    <delete dir="${build.dir}/classes/${componentName}"/>
    <delete dir="${build.dir}/work/${componentName}"/>
</target>

<target name="copy" depends="setenv"/>

<target name="compile" depends="copy">
  <echo message="Compiling test.wsdl.marrays"/>
    <!-- MArrayTests Test -->
    <wsdl2java url="${axis.home}/src/test/java/test/wsdl/marrays/MArrayTest.wsdl"
               output="${build.dir}/work"
               deployscope="session"
               serverSide="yes"
               skeletonDeploy="yes"
               testcase="yes">
        <mapping namespace="urn:MArrayTests" package="test.wsdl.marrays"/>
    </wsdl2java>

    <copy todir="${build.dir}/work/test/wsdl/marrays" overwrite="yes">
      <fileset dir="${axis.home}/src/test/java/test/wsdl/marrays">
        <include name="*TestCase.java"/>
        <include name="*Impl.java"/>
      </fileset>
    </copy>

    <javac srcdir="${build.dir}/work/test/wsdl/marrays" destdir="${build.dest}" debug="${debug}" nowarn="${nowarn}" source="${source}" fork="${javac.fork}" verbose="true">
      <classpath>
        <path refid="classpath"/>
      </classpath>
      <include name="**/*.java"/>
    </javac>
</target>

<target name="run" depends="compile">
  <echo message="Running marrays test with componentName: ${componentName}" />
  <echo message="Looking for test classes in: ${build.dest}/${componentName}" />
  <antcall target="execute-Component" />
</target>

</project>
