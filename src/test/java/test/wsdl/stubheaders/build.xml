<?xml version="1.0" ?>
<!DOCTYPE project [
        <!ENTITY properties SYSTEM "../../../../../../xmls/properties.xml">
        <!ENTITY paths  SYSTEM "../../../../../../xmls/path_refs.xml">
        <!ENTITY taskdefs SYSTEM "../../../../../../xmls/taskdefs.xml">
        <!ENTITY taskdefs_post_compile SYSTEM "../../../../../../xmls/taskdefs_post_compile.xml">
        <!ENTITY targets SYSTEM "../../../../../../xmls/targets.xml">
]>

<!-- ===================================================================
<description>
   Test/Sample Component file for Axis

Notes:
   This is a build file for use with the Jakarta Ant build tool.

Prerequisites:

   jakarta-ant from http://jakarta.apache.org

Build Instructions:
   To compile
        ant compile
   To execute
        ant run

Author:
  Matt Seibert mseibert@us.ibm.com

Copyright:
  Copyright (c) 2002-2003 Apache Software Foundation.
</description>
==================================================================== -->

<project default="compile">

<property name="componentName" value="test/wsdl/stubheaders" />
        &properties;
        &paths;
        &taskdefs;
        &taskdefs_post_compile;
        &targets;


<target name="clean">
    <echo message="Removing ${build.dir}/classes/${componentName} and ${build.dir}/work/${componentName}" />
    <delete dir="${build.dir}/classes/${componentName}"/>
    <delete dir="${build.dir}/work/${componentName}"/>
</target>

<target name="copy" depends="setenv"/>

<target name="compile" depends="copy">
  <echo message="Compiling test.wsdl.stubheaders"/>
    <!-- This tests holders for complextype Bug:10247 -->
    <wsdl2java url="${axis.home}/src/test/java/test/wsdl/stubheaders/stubheaders.wsdl"
               output="${build.dir}/work"
               serverSide="yes"
               testcase="yes">
        <mapping namespace="http://echo.services" package="test.wsdl.stubheaders"/>
    </wsdl2java>

    <copy todir="${build.dir}/work/test/wsdl/stubheaders" overwrite="yes">
      <fileset dir="${axis.home}/src/test/java/test/wsdl/stubheaders">
        <include name="*TestCase.java"/>
        <include name="*Impl.java"/>
      </fileset>
    </copy>

    <javac srcdir="${build.dir}/work/test/wsdl/stubheaders" destdir="${build.dest}" debug="${debug}" nowarn="${nowarn}" source="${source}" fork="${javac.fork}" verbose="true">
      <classpath>
        <path refid="classpath"/>
      </classpath>
      <include name="**/*.java"/>
    </javac>
</target>

<target name="run" depends="compile">
  <antcall target="execute-Component" />
</target>

</project>
